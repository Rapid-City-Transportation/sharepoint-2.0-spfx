export interface ITeamLeadHubProps {
  /** Header for the Weekly Focus card, e.g. "Week of May 20". */
  weekTitle: string;
  /** Date-range subtitle, e.g. "May 18 to May 24, 2026". */
  weekDateRange: string;
}
