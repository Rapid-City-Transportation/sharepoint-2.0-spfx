export interface IDispatchHubProps {
  /** Eyebrow line above the updates banner title (property pane). */
  bannerEyebrow: string;
  /** Updates banner title (property pane). */
  bannerTitle: string;
}
